# Requirements Traceability Matrix / 需求追踪矩阵

| Requirement | Primary tasks | Main tests |
|---|---|---|
| FR-01 Profile management | M0-004, M1-002, M1-004 | domain contract validation, JSON/YAML round trip, unknown-field policy, schema round trip, CRUD, import/export; M1-002 CRUD + update backup/updatedAt stamp, path-traversal id rejection, JSON/YAML import round trip + strict/allowRename/oversize/hostile-id, export sanitize (token/secret keys → null, absolute private path → `<private>`), uncorrupted-path error leak guard; M1-004 CLI list summary + --model/--task filter, show sanitized doc, create from stdin(-)/YAML-file with --name id-match + duplicate-id fast-fail + rollback on mismatch, edit deep-merge patch + id-immutable + template mode, clone fresh metadata, delete --yes, import extension routing + --allow-rename, export literal document + -o (not wrapped by --json) |
| FR-02 Hardware discovery | M0-004, M1-001 | HardwareProfile contract (probe-failure tolerance, bounds), platform fixtures, partial failure; M1-001 probe fragments (os/cpu/memory/gpu/volumes/power), nvidia-smi + WMI/registry fallback fixtures, exec runner timeout/ENOENT/maxBuffer, redaction guard, fingerprint determinism/order-insensitivity; M1-001 re-test (2026-09-05): SCHEMA_VERSION 1→2 external-volume contract — VolumeInfo `driveType`/`bus`/`external`/`model` (nullable-optional + passthrough), v1→v2 forward migration (deep-copy/stamp/validate, source untouched on failure) + future-version reject tests, four-table WMI join (`Win32_LogicalDisk` DriveType∈{2,3}, `Win32_DiskDrive`+MediaType, both partition associations) via class-aware `parseWmiRef`/`toWmiRefPair`/`joinVolumeDetails` pure functions, `classifyExternal` (DriveType2→true; USB/USBSTOR/MediaType EXTERNAL|REMOVABLE→true; non-USB evidence→false; none→null), enrichment tables optional (volumes always enumerate, columns null), statfs single-volume fallback; real-machine verified — RTX 5060 Ti nvidia-smi success path (`gpus` populated), C:/D: external:false (SCSI NVMe), E: external:true via MediaType label (JMicron bridge), CLI human output `(external)` marker, locale keys `hardware.volumeExternal` ×2, domain:schema:check gate green |
| FR-03 Capability matrix | M0-004, M0-005, M1-003 | CapabilityMatrix contract enums/TTL, SDK/REST/CLI probes, unsupported field; M0-005 three-source probe (REST v1 `GET /api/v1/models` → `lms status`/`ls --json` → guarded SDK import), exact/unknown/unavailable semantics, per-source matrices + operational `ProbeOps` record, TTL cache + `force`, error classification (unreachable/auth/timeout/unsupported/parse/process/health/internal), mock only under explicit selection, router fallback to `unreachableRuntime`, real-host `probe:real` redacted record (LOCAL-ONLY `~/.lmps/realm/`); M1-003 CLI wiring — REST on-line path (models/current/snapshot + profileId reverse-lookup), offline `LM_UNREACHABLE` exit 4 on all three read ports, CLI-only read fallback (`lms ls --json`) with honest current/snapshot failure, `captureSnapshot` reuse, CLI architecture guard widened to 6 workspace packages; real server field-support evidence pending machine block — truthful offline record |
| FR-04 Activation | M0-004, M1-005 | ActivationTransaction contract/stages, state failure injection, idempotency, rollback; M1-005 runner state machine (validating→estimating→locking→unloading-conflicts→loading→health-checking→active), per-stage fault injection, recovered vs unrecovered exit codes (3 vs 5, success 0, cancel 2), idempotent already-active path, cancellation via AbortSignal, per-stage timeout (`--timeout` applied), lock acquire/release + file-lease lock, redacted transaction log, snapshot capture, `apply --yes` no-silent-switch gate; M1-005 production wiring — deps.ts ActivationSeam (cached router bundle for a consistent host view, `LMPS_ADAPTER=mock` explicit-only mock, `createFileLock` process.pid owner + 30-min lease, `createCliEstimatePort` rough-fallback feed, ndjson sink at `<LMPS_HOME>/logs/transactions.ndjson`, `resolveRootDir` explicit rootDir precedence); apply-wiring tests: mock end-to-end exit 0 + redacted log + lock released, offline `LM_UNREACHABLE` exit 4 with no log, createDefaultDeps over a real temp dir; real-machine `apply` pending server (truthful) |
| FR-05 Storage | M0-004, M1-002 | migration forward + reject-future (pure, rollback-safe), crash simulation, backup restore; M1-002 temp+fsync+atomic rename fault-injection (write/fsync/rename → STORE_IO_FAILED, original preserved, no temp residue), backup rotation (default 20, 1–200 RangeError), recover() main-corrupt/backup / main-missing resurrection / temp cleanup, migration rollback leaves file+backups untouched, import size limit (STORE_LIMIT_EXCEEDED), config-vs-runtime error split |
| FR-06 CLI | M1-004, M1-005, M1-003 | eight commands (profile/hardware/lang/doctor/models/current/snapshot/apply), machine envelope shape `{product,api,ok,locale,command,data|error}` + stable error codes, exit-code mapping table (0/2/3/4/5/6/10 — 2/3/5 effective for activation outcomes), global flags any position (--json/--lang/--verbose/--no-color/--timeout), stream isolation (human success/envelope → stdout, human errors+verbose → stderr, JSON stderr empty), export/edit-template literal-document exception, unwired seams → CAPABILITY_UNSUPPORTED exit 6, doctor checks complete → exit 0 with summary.ok, locale runtime resolution + persistence, LMPS_HOME, usage/help; M1-005 `apply <id> [--yes]` with `--timeout` applied as stage timeout; M1-003 models/current/snapshot wired to adapter router (REST v1 primary, CLI read fallback), offline/auth/timeout → `LM_UNREACHABLE` exit 4 (bilingual `error.lmUnreachable`); M1-005 production wiring: `apply` runs the real state machine — mock end-to-end exit 0, offline `LM_UNREACHABLE` exit 4 with `--timeout 10` accepted, real-process smoke verified (isolated LMPS_HOME, no log/lock residue), `apply` no longer returns exit 6 |
| FR-07 Optimizer | M0-004, M1-006, M2-001, M2-002 | LoadEstimate contract bounds, deterministic rules, unsafe filtering; M1-006 official estimate (`lms load --estimate-only` adapter: estimateArgs + lenient `parseLmsEstimateValues` on documented table/`label:` shapes, timeout/exit/parse classification, 30s CLI budget), explicit `provider:'rough'` fallback (VRAM/RAM null + warning, never silent approval) and `createCliEstimatePort` (exact → rough on non-internal errors, internal/unexpected rethrow); fixtures 17 tests; real output contract pending server (blocked as M0-005); M2-001 Task Profiles and Rules — `TASK_KINDS` (11 FR-07 kinds) + optional `kind` on TaskProfile (backward-compatible, no schema bump), new `RulesDocument` contract (schemaVersion=2, `version` rule-pack field, RuleSchema constraint/hint/weights + strict variants, registry + generated JSON Schema, schema gate byte-identical), optimizer data layer (pure `loadRuleCatalog`/`validateRuleCatalog` + `SEED_RULE_CATALOG` 2026.09.1 declarative data module, rag + investment-due-diligence fully fleshed, bilingual rationale as data field, module-load strict self-check, architecture guard with presets.ts data-module CJK exemption), deterministic fixtures (41 new tests: schema accept/reject boundaries, loader/validator, seed completeness + uniqueness); M2-002 Candidate Optimizer — new `Candidate`/`Recommendation` contracts in domain registry (schemaVersion=2, passthrough + transitive-strict variants, Candidate carries full CompositeProfile/estimate/safety/score/diff/bilingual rationale; generated Candidate.schema.json + Recommendation.schema.json, 14 total, byte-identical gate), optimizer pure pipeline (deterministic quantile draft generation 3–6 + `toCapabilityPath` bare-name bridge + hard-constraint filter minContextLength/maxConcurrency/requiredCapabilities unavailable-drop unknown-force-low, `computeSafetyMargin` floor-before-GPU-reachability precedence + rough fail-closed, `scoreCandidate` normalized weights with proxy sub-scores `measured=false` + `confidence='high'` only under exact+safe+!forceLow, `diffAgainst` field-level dotted-path null-normalized, `buildRationale` bilingual data — zero I/O, estimates map precomputed by core), core `RecommendationService` orchestration (rule match by `profile.task.kind`, kindless/custom → empty + warning, new `CapabilityPort`/`HardwarePort` + reused `EstimatePort`, estimate throw → drop candidate + `estimate-failed:<id>`, abort → ACTIVATION_CANCELED), CLI `lmps optimize <id> [--yes] [--json]` (no `--yes` renders candidates + `optimize.noSafeCandidate` exit 0 when none; `--yes` saves head candidate as `<baseline>-<ruleVersion>` with `validation.source='rule-recommended'` + audit ndjson at `<LMPS_HOME>/logs/optimizations.ndjson`, refuses low-confidence/no-safe at exit 4, never auto-activates; unspecified/unrecoverable id → exit 4, seam null/undefined → exit 6), i18n keys (12 new, resources.generated.ts 109 keys, gate green) + CLI_SPEC `## optimize`; 77 new deterministic tests covering exact mid-draft scoring breakdown, strict-transitive rejection, flooring precedence, dual-GPU VRAM sum, rough/over-vram/estimate-missing/architecture-mismatch drops and CLI show-only/save/refuse/exit-code paths (64 files / 674 tests); real-machine optimize smoke (2026-09-05, `lms server start` on 127.0.0.1:1234, qwen/qwen3.5-9b rag baseline in isolated LMPS_HOME): show-only produced 6 exact/safe candidates ranked non-increasing with head `rag-smoke-low-high`, all low-confidence under `unknown-capability` (REST API auth 401 → runtime fields conservatively unknown → `--yes` refused at exit 4 with zero side-effects) — regression fixes surfaced and landed: `estimateArgs` gained `--yes` (without it `lms load --estimate-only` opens the interactive TUI and hangs non-TTY spawns) and the estimate parser now scans merged stdout+stderr (real host writes the human-readable estimate to stderr) with a dedicated stderr-channel unit test; full suite 675 tests green |
| FR-08 Benchmark | M0-004, M2-003 | BenchmarkResult contract bounds, cancel, fingerprint invalidation |
| FR-09 Desktop Web GUI and tray | M0-006, M3-001, M3-002, M3-003 | Web GUI IPC, bilingual E2E, scaling, layout/interaction review, tray |
| FR-10 Hook and rules | M4-001, M4-002 | auth, lock, session stability |
| FR-11 i18n | M0-003 and every UI task | key parity, generated-key freshness, hard-coded string scan, normalization/detection, fallback, live switch, persistence, CLI bilingual output, unlocalized machine JSON; M1-004 84 keys × 2 locales, full CLI usage/error/profile/hardware/doctor text through keys, per-key fallback on missing key, byte-identical machine data across locales |
| FR-12 independent reuse | M0-002 and every port | provenance, license, no live repo dependency |
| Distribution and visibility governance | M0-002 follow-up, M3-004 | file classification review, policy/secret scan before first push, release allowlist, manifest and checksums |
| Release packages (Windows + macOS) | M3-004 | platform artifact matrix, clean-machine install/uninstall/update/rollback, signing and notarization evidence |
| Security | all; M3-004 release gate | import fuzz, redaction, loopback, SBOM |
