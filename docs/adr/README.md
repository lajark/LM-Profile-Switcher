# Architecture Decision Records

仅为同时满足以下条件的决定创建 ADR：难以逆转、缺少背景时令人意外、确实存在取舍。草稿先经内部评审（决策草稿存于本机，LOCAL-ONLY），接受后再提升到本目录。

编号使用四位递增序号；状态为 `proposed | accepted | superseded | rejected`。已接受 ADR 不静默改写，后续变化通过新 ADR supersede。

