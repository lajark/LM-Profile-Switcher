{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M3-001 desktop shell: the verification window spawns the packaged lmps-sidecar binary and writes/reads its stdio; core:default covers webview event listen/emit for the sidecar://status channel.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    "shell:allow-stdin-write",
    "shell:allow-kill",
    "shell:allow-execute"
  ]
}