engine-strict=true
prefer-workspace-packages=true
shared-workspace-lockfile=true
# Pin pnpm's content-addressable store inside the project (gitignored) so
# installs never scatter a store into the drive root (see D:\.pnpm-store).
store-dir=.pnpm-store
